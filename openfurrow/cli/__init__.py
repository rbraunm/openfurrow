# SPDX-License-Identifier: Apache-2.0
"""Command-line interface for OpenFurrow.

The entry point is ``openfurrow.cli.main:main`` (see the console script in
pyproject). It is intentionally not re-exported here, so ``python -m
openfurrow.cli.main`` does not import the module twice.
"""
